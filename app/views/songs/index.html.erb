<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" type="text/css" href="/style.css"/>
    <link href="https://fonts.googleapis.com/css?family=Raleway|Shrikhand" rel="stylesheet">
  </head>
  <body>
    <h1> Playlist </h1>
    <div class="songs-container">
      <% @songs.each do |song| %>
      <ul class="song-item">
        <img src="https://raw.githubusercontent.com/turingschool-examples/set-list-song-index-B2/master/vinyl.jpg" alt="Vinyl Photo by Adrian Korte"/>
        <li><%= song.title %></li>
        <li><%= song.length %> seconds</li>
        <li>Play Count: <%= song.play_count %></li>
      </ul>
      <% end %>
    </div>
  </body>
</html>
